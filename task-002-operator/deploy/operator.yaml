apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-002-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: task-002-operator
  template:
    metadata:
      labels:
        name: task-002-operator
    spec:
      serviceAccountName: task-002-operator
      containers:
        - name: task-002-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - task-002-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "task-002-operator"
